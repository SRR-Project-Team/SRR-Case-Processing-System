REACT_APP_API_URL=https://your-backend.example.com
REACT_APP_VERSION=1.0.0

# Copy to .env.production (keep out of git)
# Firebase predeploy runs: npm --prefix srr-chatbot run build
# CRA picks up REACT_APP_* at build time and exposes them client-side.
















